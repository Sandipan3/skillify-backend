name: Keep Render Service Awake
on: 
    schedule:
       - cron: "*/10 * * * *"
    workflow_dispatch: 
        
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Service
        run : |
         curl -s https://skillify-backend-nzhx.onrender.com/api/v1/ping > /dev/null
